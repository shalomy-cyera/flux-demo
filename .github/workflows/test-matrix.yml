on:
  workflow_dispatch:

name: Build Services

jobs:
  pre_build:
    name: Pre build
    runs-on: ubuntu-20.04
    outputs:
      matrix: ${{ steps.format_input.outputs.matrix }}
    steps:
      - name: Format Input
        id: format_input
        run: |
          echo "matrix={\"packages_to_build\":[\"shalom,gal\"]}" >> $GITHUB_OUTPUT
  test_matrix:
    name: Test Matrix
    runs-on: ubuntu-20.04
    needs: pre_build
    strategy:
      matrix: ${{ fromJson(fromJson(needs.pre_build.outputs.matrix).matrix) }}
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Set inputs
        id: set_inputs
        shell: bash
        run: |
          echo ${{ matrix.packages_to_build }}
